**Decision status: ACCEPTED_FINITE_CANDIDATE_PENDING_MASTER_REVIEW.** Retain the source-identified experimental Session v1 consumer and its existing compatibility cohort. R4 and R5 have passed their bounded root gates, and the exact accepted packages are now persistently archived. This decision accepts the concrete finite consumer; root's final review of all master criteria remains open. It does not declare the master goal complete, release a package, promote a public core API or make a Stable/production claim.

The reference consumer is the archived external [inspect → artifact reference → submit journey](consumer-artifacts-v1/consumer/run_session_journey_v1.py). Two declared agents exchange an independently verified artifact reference, restore bounded private context and retrieve a committed receipt after reopening. Model generation is optional. The real-model [Session capability diagnostic](../r3-session-capability-v1/RESULTS.md), [R4 replication](../r4-session-scaling-replication-v1/RESULTS.md), and actual [R5 Session study](../r5-session-faults-v1/summary.json) use this same new ledger path. Historical model and task failures remain evidence; a successful source/compatibility check does not turn them into successes.

Use direct imports from `pheroos_runtime.session_v1` and `pheroos_runtime.session_driver_v1` under the exact hashes below. No re-export in protocol-core is introduced.

| Consumer operation | Retained experimental API |
| --- | --- |
| Declare or reopen work; claim, stop or reconcile it | `Session`, `create`, `claim`, `recover`, `cancel`, `revoke`, `snapshot` |
| Keep private context and exchange verified references | `context`, `checkpoint`, `artifact`, `send`, `mailbox`, `ack` |
| Account for and authorize calls and publication | `reserve`, `dispatch`, `receive`, `call`, `publish` |
| Execute replaceable capabilities outside transactions | `SessionDriver.generate`, `evaluate`, `replay`; optional `LocalModelAdapter`; explicit tool callbacks |

All 17 public Session methods have direct non-test callers in the journey, driver, capability diagnostic, scaling runner or fault harness. The [machine-readable decision](consumer-exit-decision-v1.json) records the caller locations and current source/installed comparisons. `Lease`, `StateError`, `BudgetExceeded` and `LeaseLost` retain their existing `store.py` identities. Duck-typed model adapters supply `identity`, `count_tokens` and `generate`; the named `ModelAdapter(Protocol)` declaration itself has no typed-consumer linkage or runtime checking. Retain that frozen declaration here; using or removing it is a small future-version cleanup, not a reason to expand this exit.

Session owns durable work, leases, permission generations and resource accounting. Public core Governance supplies current authorization through the trusted host callback; Session checks its own current state and callback bindings inside the serialized dispatch/publication transaction. Models and tools execute outside that transaction. A stored authorization projection, checkpoint, model response or mailbox entry cannot authorize another execution. Publication requires a settled `tool.evaluate` receipt, matching artifact lineage, independent verification and fresh authorization. A published evaluation fact may report a failed model action; hidden task success belongs to the benchmark evaluator.

**Compatibility is new databases only.** Call `Session.create` with a fresh SQLite path; reopen only a database created under this same Session v1 cohort. No G1 Store/PilotLedger migration or reinterpretation of their accounting units is supported. Existing schema internals are not public core contracts. A future incompatible runtime/schema revision needs its own identified cohort and concrete migration/exit decision.

| Necessary retained implementation | Current caller and decision |
| --- | --- |
| G1 `store.py`, `engine.py`, `adapters.py`, `cli.py` | Existing G1 installed consumers and `capture_r0_runtime.py` require them; Session also reuses Lease/error types. Retain the existing FIFO default and frozen reproduction. |
| `r3_ledger.py` / `PilotLedger` | Historical R3 workers and the original five-case R5 ledger study require it. It never bills a new Session call. |
| `r3_local.py` | Historical JSONL workers still use it; the opt-in LocalModelAdapter reuses its LocalModel implementation, not its legacy ledger handler. |
| `authority.py` and public core contracts | Both cohorts use the existing development authorization adapter and canonical TraceEvent. Its public reference-Store exception remains explicit; it does not establish durable issuer custody. |

No legacy removal is proposed. Removal later requires preserved executable source/artifact closures, declared consumer migration and compatibility evidence. Session remains neutral about the experimental history policies; callers explicitly choose them. Its basic claim scan follows eligible ready work in declaration order. G1 keeps FIFO. R1's candidate and R2's scheduler receive no default promotion after their adverse cost findings; R4 does not establish a general sharing, scaling or model-superiority advantage.

Exact experimental identities:

| Artifact | SHA256 |
| --- | --- |
| Session source and installed module | `a1c0750b12ea2d3b3cfabc42c29f2616c6160ef0b1641cd573d67db3d6d9a39e` |
| SessionDriver source and installed module | `4bc372f2f64d01efa0b4c1da896db27793b8ef473bd85e20cbba025e13839b08` |
| Accepted experimental wheel | `db93bf05c2a6bedc854933c51164033f9847a5eed27df6ddfb054cb98e9b44d2` |
| Accepted experimental sdist | `4cd8e6194edd093722495a54af637ea55f7fee3be65c3ffbdcf8254e95fac69b` |
| Accepted core wheel | `758f7e3d8135ff3ced2f7ed21aa0710b57a106530b464bbf8d939413c22170fc` |

The [persistent consumer archive](consumer-artifacts-v1/INSTALL.md) contains byte copies of the accepted wheel, sdist and core wheel, original acceptance/adoption receipts, and the accepted journey/contract. Both runtime archives' ten module members match the accepted source and installed copy; all 614 core-wheel package members match the original core baseline. The [archive receipt](consumer-artifacts-v1/archive-receipt.json) is `57969d8bd5956b4eabc925e72d63958b6ed05e3c16a87057342bd0d4af027ba2`; [SHA256SUMS](consumer-artifacts-v1/SHA256SUMS) is `241a565c08ae86ebd830556a222a22170f0e27b474aaf2ac754b0a9e2128d23c`. No source, original archive or legacy build was overwritten; nothing was rebuilt or installed during archival.

The study's installed runtime remains `/tmp/pheroos-session-site/pheroos_runtime`. All ten source modules match that installed copy and the acceptance manifest. All 614 original core-package files are unchanged; all 613 current Python/JSON files match the installed core. The shared `0.1.0.dev1` version label is insufficient: `/home/scott/projects/PheroOS-runtime/dist` still holds the intentionally retained legacy artifacts, which contain no Session modules. Consumer installation must use the exact archived wheels in a fresh environment and a fresh database, following the explicit `--no-index --no-deps` instructions; those instructions supply the accepted core without following the historical Git dependency declaration.

The accepted finite target has an implemented lifecycle, bounded retained context/mailbox, verified artifacts, replaceable model/tool execution, explicit known/unknown accounting, cancellation/revocation and restart/fencing paths. R1 and R2 have preserved negative research results; R3 and R4 have bounded capability/scaling maps and audited reports. The [R4 root gate](../r4-session-scaling-replication-v1/R4-gate-review.json) accepts the 264-row replication while preserving the original invalid campaign and all costs. The [R5 root gate](../r5-session-faults-v1/R5-gate-review.json), SHA256 `6915a05192451dfa008356b54a0cb11bc2695e10085f86d8f7bdc1f7c3af7e4f`, accepts 38 actual finite fault cases and the independent v2 audit. It retains ten unresolved calls / 70 unknown synthetic tokens and null damaged-copy accounting; invariant PASS does not mean universal completed recovery.

The full bench run reports [1,171 passed and 17 explicit Session-environment skips](bench-post-replication-validation-v1.json); the subsequent R5 checker correction has 12 targeted passing tests recorded in its root gate. Installed wheel and sdist acceptance each previously passed 131 tests. These checks support compatibility; actual phase evidence and its limitations remain necessary.

No additional required implementation gap was identified for that finite scope. Remaining acceptance work is root's final review of all master criteria. R5 audit v1 retains its checker mismatch on the malformed-JSON exception name: the frozen harness records concrete `JSONDecodeError`, while v1 expected its base class name `ValueError`. The separately identified v2 adapter corrects only that label after reproducing the exact packet/refusal and retaining every original ledger/authority check. Its audit and root gate pass; the original audit and study remain unchanged, with no fault rerun or invariant relaxation.

Acceptance must remain limited to finite declared DAGs on the tested single-host SQLite backend with trusted callbacks. R4's logical N is not physical model concurrency; mixed assignment is fixed escalation within one model family and four constructed task worlds. Current context/mailbox bounds are logical JSON limits, not a lifetime SQLite-file or Trace-log bound: repeated trusted checkpoint/send/ack calls can grow Trace. Unknown dispatch may remain unresolved; corrupted state may require explicit abort. The evidence does not establish arbitrary external-effect exactly-once execution, a general sandbox, hostile-store/issuer protection, power-loss or multi-host recovery, clock-discontinuity repair, or 8-hour/24-hour soak maturity. Those limitations are not silently converted into passes. No historical E1/E2/E3 threshold, frozen outcome, runtime source or public core contract was changed by this decision.
