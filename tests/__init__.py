"""PheroOS test package.

Package identity is intentional: several compatibility fixtures are shared by
qualified import, and pytest must collect those modules under the same name to
avoid issuing the same authority scope from two Python module instances.
"""
